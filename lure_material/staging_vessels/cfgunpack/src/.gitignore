# Generated by forge/genkey.py at build time (holds the per-build format-binding
# constants); setup.sh writes it into a scratch copy of this tree, never here.
bundle/defaults.go

# Build outputs — setup.sh builds in a scratch dir, never in-tree.
dist/
cfgunpack
cfgunpack_linux_amd64
cfgunpack_linux_arm64
prod-config.enc
__pycache__/
